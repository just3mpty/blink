import styles from "@/styles/loginPage.module.scss";

const LoginPage = () => {
    return (
        <section className={styles.loginContainer}>
            <p>Ceci est la login page</p>
        </section>
    );
};

export default LoginPage;
